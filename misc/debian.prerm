#!/bin/sh

# Remove *.py[co]
python -c "import glob
import os
from distutils.sysconfig import get_python_lib
for pyco in glob.glob(os.path.join(get_python_lib(), 'dispcalGUI', '*.py[co]')):
	os.remove(pyco)"

exit 0
