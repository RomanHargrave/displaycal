Source: ${DEBPACKAGE}
Section: graphics
Priority: extra
Maintainer: ${MAINTAINER} <${MAINTAINER_EMAIL}>
Build-Depends: debhelper (>= 4.1.16), gcc, python (>= ${PY_MINVERSION}), python (<= ${PY_MAXVERSION}), python-all-dev, libxinerama-dev, libxrandr-dev, libxxf86vm-dev

Package: ${DEBPACKAGE}
Architecture: any
Depends: ${shlibs:Depends}, python (>= ${PY_MINVERSION}), python (<= ${PY_MAXVERSION}), python-wxgtk2.8 (>= ${WX_MINVERSION}), python-numpy (>= 1.0)
Description: ${SUMMARY}
${DESC}
