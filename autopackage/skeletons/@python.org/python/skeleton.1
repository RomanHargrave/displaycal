[Meta]
RootName: @python.org/python
DisplayName: Python Interpreter
ShortName: python
Skeleton-Author: Florian Höch <florian.hoech@gmx.de>
Skeleton-Version: 2

[Notes]
Tests only the "system" python executable. For example if python 2.5.2
is installed, INTERFACE_VERSIONS is "2.5" and SOFTWARE_VERSIONS is set to
the output of "python -V".

[Test]
SOFTWARE_VERSIONS=$( locateCommand python -V 2>&1 | cut -f2 -d' ' )
if [[ "$?" != "0" ]]; then
    INTERFACE_VERSIONS=""
else
    INTERFACE_VERSIONS=$( echo $SOFTWARE_VERSIONS | awk -F. '{print $1 "." $2}' )
fi
